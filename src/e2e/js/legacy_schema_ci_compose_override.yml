# Ordinary localdb regression data is still schema 3.0.0. This override is
# used only by the legacy CircleCI Playwright job; WSI validation starts its
# own 3.4.0 fixture without this file.
services:
  cbioportal:
    environment:
      JAVA_TOOL_OPTIONS: "-Ddb.version=3.0.0"
