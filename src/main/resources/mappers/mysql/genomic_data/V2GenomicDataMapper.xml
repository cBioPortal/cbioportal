<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.cbioportal.infrastructure.repository.genomic_data.V2GenomicDataMapper">

    <!-- for /molecular-profile-sample-counts/fetch (returns GenomicDataCount) which will then be converted to clinicalDataCountItems -->
    <select id="getMolecularProfileSampleCounts" resultType="org.cbioportal.legacy.model.GenomicDataCount">
        SELECT REPLACE(genetic_profile.stable_id,
        CONCAT(sample_derived.cancer_study_identifier,'_'), '') AS value,
        genetic_profile.name AS label,
        count(sample_profile.genetic_profile_id) AS count
        FROM sample_profile
        LEFT JOIN sample_derived ON sample_profile.sample_id=sample_derived.internal_id
        LEFT JOIN genetic_profile on sample_profile.genetic_profile_id = genetic_profile.genetic_profile_id
        <where>
            <include
                    refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
        </where>
        GROUP BY genetic_profile.stable_id, genetic_profile.name, sample_derived.cancer_study_identifier
    </select>

    <select id="getGenomicDataBinCounts" resultType="org.cbioportal.legacy.model.ClinicalDataCount">
        <bind name="profileType" value="genomicDataBinFilters[0].profileType"/>
        <!-- get all non-NA value samples. A caveat here is that if user select only 'NA', this query will return empty (null) thus we need the 2 coalesce() below to handle this case -->
        WITH genomic_numerical_query AS (
        SELECT
        concat(hugo_gene_symbol, profile_type) AS attributeId,
        <include
                refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.normalizeAttributeValue">
            <property name="attribute_value" value="alteration_value"/>
        </include>
        AS value,
        cast(count(value) as SIGNED) AS count
        FROM genetic_alteration_derived
        <where>
            <!-- Table creation in clickhouse.sql has ensured no NA values but extra caution is always appreciated -->
            <include
                    refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.normalizeAttributeValue">
                <property name="attribute_value" value="alteration_value"/>
            </include>
            != 'NA' AND
            profile_type = #{profileType} AND
            <include
                    refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
            <foreach item="genomicDataBinFilter" collection="genomicDataBinFilters" open=" AND (" separator=" OR "
                     close=")">
                hugo_gene_symbol = #{genomicDataBinFilter.hugoGeneSymbol}
            </foreach>
        </where>
        GROUP BY hugo_gene_symbol, profile_type, value
        ),
        genomic_numerical_sum AS (
        SELECT
        attributeId,
        sum(count) as genomic_numerical_count
        FROM genomic_numerical_query
        GROUP BY attributeId
        )
        SELECT * FROM genomic_numerical_query
        UNION ALL
        <!-- The NA count is specially caculated using total sample count minus non-NA count, therefore
        these 2 coalesces are here in case the non-NA subquery returned empty results and we need to provide properties needed to construct the target object -->
        SELECT
        coalesce((SELECT attributeId FROM genomic_numerical_sum LIMIT 1),
        concat(#{genomicDataBinFilters[0].hugoGeneSymbol}, #{profileType})) as attributeId,
        'NA' as value,
        cast(((SELECT * FROM (<include
            refid="org.cbioportal.infrastructure.repository.sample.V2SampleMapper.getFilteredSampleCount"/>
        )) - coalesce((SELECT genomic_numerical_count FROM genomic_numerical_sum LIMIT 1), 0)) as SIGNED) as count
    </select>

    <!-- for /genomic-data-counts/fetch - (returns GenomicDataCountItem objects) -->
    <select id="getCNACounts" resultMap="GenomicDataCountItemResultMap">
        <bind name="profileType" value="genomicDataFilters[0].profileType"/>
        <!-- get all non-NA value samples. A caveat here is that if user select only 'NA', this query will return empty (null) thus we need the 2 coalesce() below to handle this case -->
        WITH cna_count_query as (
            SELECT
                hugo_gene_symbol as hugoGeneSymbol,
                #{profileType} as profileType,
                CASE
                    WHEN alteration_value = '2' THEN 'Amplified'
                    WHEN alteration_value = '1' THEN 'Gained'
                    WHEN alteration_value = '0' THEN 'Diploid'
                    WHEN alteration_value = '-1' THEN 'Heterozygously deleted'
                    WHEN alteration_value = '-2' THEN 'Homozygously deleted'
                    ELSE 'NA'
                END AS label,
                toString(alteration_value) as value,
                cast(count(*) as SIGNED) as count
            FROM genetic_alteration_derived
            <where>
                <!-- Table creation in clickhouse.sql has ensured no NA values but extra caution is always appreciated -->
                <include
                        refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.normalizeAttributeValue">
                    <property name="attribute_value" value="alteration_value"/>
                </include>
                != 'NA' AND
                profile_type = #{profileType} AND
                <include
                        refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
                <foreach item="genomicDataFilter" collection="genomicDataFilters" open=" AND (" separator=" OR " close=")">
                    hugo_gene_symbol = #{genomicDataFilter.hugoGeneSymbol}
                </foreach>
            </where>
            GROUP BY
            hugo_gene_symbol,
            alteration_value
        ),
        cna_sum AS (
            SELECT
            hugoGeneSymbol,
            sum(count) as cna_count
            FROM cna_count_query
            GROUP BY hugoGeneSymbol
        )
        SELECT * FROM cna_count_query
        UNION ALL
        <!-- The NA count is specially caculated using total sample count minus non-NA count, therefore
        these 2 coalesces are here in case the non-NA subquery returned empty results and we need to provide properties needed to construct the target object -->
        SELECT
            coalesce((SELECT hugoGeneSymbol FROM cna_sum LIMIT 1), #{genomicDataFilters[0].hugoGeneSymbol}) as
            hugoGeneSymbol,
            #{profileType},
            'NA' as label,
            'NA' as value,
            cast(((SELECT * FROM (<include
                refid="org.cbioportal.infrastructure.repository.sample.V2SampleMapper.getFilteredSampleCount"/>
            )) - coalesce((SELECT cna_count FROM cna_sum LIMIT 1), 0)) as SIGNED) as count
    </select>

    <!-- for /mutation-data-counts/fetch - (returns GenomicDataCountItem objects) mutation type counts table part-->
    <select id="getMutationCountsByType" resultMap="GenomicDataCountItemResultMap">
        SELECT
            hugo_gene_symbol as hugoGeneSymbol,
            'mutations' as profileType,
            replace(mutation_type, '_', ' ') as label,
            mutation_type as value,
            count(*) as count,
            count(distinct(sample_unique_id)) as uniqueCount
            FROM genomic_event_derived
        <where>
            <include
                    refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
            AND variant_type = 'mutation'
            <foreach item="genomicDataFilter" collection="genomicDataFilters" open=" AND (" separator=" OR " close=")">
                hugo_gene_symbol = #{genomicDataFilter.hugoGeneSymbol}
            </foreach>
        </where>
        GROUP BY
        mutation_type,
        hugo_gene_symbol
    </select>

    <select id="getMutations" resultType="org.cbioportal.legacy.model.Mutation">
        SELECT
            cancer_study.cancer_study_identifier AS "studyId",
            CONCAT(cancer_study.cancer_study_identifier, '_', sample.stable_id) AS "sampleId",
            CONCAT(cancer_study.cancer_study_identifier, '_', sample.stable_id) AS sample_unique_id,

            gene.entrez_gene_id AS "gene.entrezGeneId",
            gene.hugo_gene_symbol AS "gene.hugoGeneSymbol",

            mutation_event.CHR AS "chr",
            mutation_event.START_POSITION AS "startPosition",
            mutation_event.END_POSITION AS "endPosition",
            mutation_event.REFERENCE_ALLELE AS "referenceAllele",
            mutation_event.TUMOR_SEQ_ALLELE AS "tumorSeqAllele",
            mutation_event.PROTEIN_CHANGE AS "proteinChange",
            mutation_event.MUTATION_TYPE AS "mutationType",
            mutation_event.NCBI_BUILD AS "ncbiBuild",
            mutation_event.VARIANT_TYPE AS "variantType",
            mutation_event.REFSEQ_MRNA_ID AS "refseqMrnaId",
            mutation_event.PROTEIN_POS_START AS "proteinPosStart",
            mutation_event.PROTEIN_POS_END AS "proteinPosEnd",
            mutation_event.KEYWORD AS "keyword",
            mutation_event.DB_SNP_RS AS "dbSnpRs",
            mutation_event.DB_SNP_VAL_STATUS AS "dbSnpValStatus",

            mutation.ENTREZ_GENE_ID AS "entrezGeneId",
            mutation.CENTER AS "center",
            mutation.MUTATION_STATUS AS "mutationStatus",
            mutation.VALIDATION_STATUS AS "validationStatus",
            mutation.TUMOR_ALT_COUNT AS "tumorAltCount",
            mutation.TUMOR_REF_COUNT AS "tumorRefCount",
            mutation.NORMAL_ALT_COUNT AS "normalAltCount",
            mutation.NORMAL_REF_COUNT AS "normalRefCount",
            mutation.AMINO_ACID_CHANGE AS "aminoAcidChange",
            mutation.ANNOTATION_JSON AS "annotationJSON",
            mutation.MATCHED_NORM_SAMPLE_BARCODE as "matchedNormSampleBarcode",
            mutation.MATCH_NORM_SEQ_ALLELE1 as "matchNormSeqAllele1",
            mutation.MATCH_NORM_SEQ_ALLELE2 as "matchNormSeqAllele2",
            mutation.TUMOR_VALIDATION_ALLELE1 as "tumorValidationAllele1",
            mutation.TUMOR_VALIDATION_ALLELE2 as "tumorValidationAllele2",
            mutation.MATCH_NORM_VALIDATION_ALLELE1 as "matchNormValidationAllele1",
            mutation.MATCH_NORM_VALIDATION_ALLELE2 as "matchNormValidationAllele2",
            mutation.VERIFICATION_STATUS as "verificationStatus",
            mutation.SEQUENCING_PHASE as "sequencingPhase",
            mutation.SEQUENCE_SOURCE as "sequenceSource",
            mutation.VALIDATION_METHOD as "validationMethod",
            mutation.SCORE as "score",
            mutation.BAM_FILE as "bamFile",
            mutation.SEQUENCER as "sequencer",
        FROM mutation
        INNER JOIN mutation_event AS me ON mutation.mutation_event_id = me.mutation_event_id 
        LEFT JOIN gene ON mutation.entrez_gene_id = gene.entrez_gene_id
        INNER JOIN sample ON mutation.sample_id = sample.internal_id
        INNER JOIN patient ON sample.PATIENT_ID = patient.INTERNAL_ID
        INNER JOIN cancer_study ON patient.CANCER_STUDY_ID = cancer_study.CANCER_STUDY_ID
        <trim prefix="HAVING" prefixOverrides="AND">
            <include refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
        </trim> 
    </select>

    <resultMap id="GenomicDataCountItemResultMap" type="org.cbioportal.legacy.model.GenomicDataCountItem">
        <result property="hugoGeneSymbol" column="hugoGeneSymbol"/>
        <result property="profileType" column="profileType"/>
        <collection property="counts" ofType="org.cbioportal.legacy.model.GenomicDataCount">
            <result property="value" column="value"/>
            <result property="label" column="label"/>
            <result property="count" column="count"/>
            <result property="uniqueCount" column="uniqueCount"/>
        </collection>
    </resultMap>

    <!-- for /mutation-data-counts/fetch (returns GenomicDataCountItem objects) mutation counts pie chart part -->
    <select id="getMutationCounts">
        WITH profiled_count as (
            SELECT count(distinct sgp.sample_unique_id)
            FROM sample_to_gene_panel_derived sgp
            JOIN gene_panel_to_gene_derived gpg ON sgp.gene_panel_id = gpg.gene_panel_id
            WHERE
            <include
                refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
            AND gpg.gene = #{genomicDataFilter.hugoGeneSymbol}
            AND sgp.alteration_type = 'MUTATION_EXTENDED'
        ),
        mutated_count as (
            SELECT count(distinct sample_unique_id)
            FROM genomic_event_derived
            WHERE
            <include
                    refid="org.cbioportal.infrastructure.repository.studyview.V2StudyViewFilterMapper.applyStudyViewFilterUsingSampleId"/>
            AND hugo_gene_symbol = #{genomicDataFilter.hugoGeneSymbol}
            AND variant_type = 'mutation'
        )
        SELECT
        cast((SELECT * FROM mutated_count) as SIGNED) as mutatedCount,
        cast(((SELECT * FROM profiled_count) - (SELECT * FROM mutated_count)) as SIGNED) as notMutatedCount,
        cast(((SELECT * FROM (<include
            refid="org.cbioportal.infrastructure.repository.sample.V2SampleMapper.getFilteredSampleCount"/>
        )) - (SELECT * FROM profiled_count)) as SIGNED) as notProfiledCount
    </select>
</mapper>